post:
  tags:
    - User
  summary: Logout
  description: |
    Used to logout and remove refresh token from header
  operationId: login
  parameters:
    - name: refreshToken
      in: cookie
      type: string
      required: true
  responses:
    '200':
      description: success
