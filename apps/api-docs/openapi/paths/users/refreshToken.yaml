post:
  tags:
    - User
  summary: Refresh Token
  description: |
    Used to get new access token
  operationId: login
  parameters:
    - name: refreshToken
      in: cookie
      type: string
      required: true
  responses:
    '200':
      description: success
